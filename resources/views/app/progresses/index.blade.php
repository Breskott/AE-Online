@extends('layouts.app')

@section('content')
<div class="container">
    <div class="searchbar mt-0 mb-4">
        <div class="row">
            <div class="col-md-6">
                <form>
                    <div class="input-group">
                        <input
                            id="indexSearch"
                            type="text"
                            name="search"
                            placeholder="{{ __('crud.common.search') }}"
                            value="{{ $search ?? '' }}"
                            class="form-control"
                            autocomplete="off"
                        />
                        <div class="input-group-append">
                            <button type="submit" class="btn btn-primary">
                                <i class="icon ion-md-search"></i>
                            </button>
                        </div>
                    </div>
                </form>
            </div>
            <div class="col-md-6 text-right">
                @can('create', App\Models\Progress::class)
                <a
                    href="{{ route('progresses.create') }}"
                    class="btn btn-primary"
                >
                    <i class="icon ion-md-add"></i> @lang('crud.common.create')
                </a>
                @endcan
            </div>
        </div>
    </div>

    <div class="card">
        <div class="card-body">
            <div style="display: flex; justify-content: space-between;">
                <h4 class="card-title">@lang('crud.progresses.index_title')</h4>
            </div>

            <div class="table-responsive">
                <table class="table table-borderless table-hover">
                    <thead>
                        <tr>
                            <th class="text-left">
                                @lang('crud.progresses.inputs.student_id')
                            </th>
                            <th class="text-left">
                                @lang('crud.progresses.inputs.car_id')
                            </th>
                            <th class="text-left">
                                @lang('crud.progresses.inputs.instructor_id')
                            </th>
                            <th class="text-left">
                                @lang('crud.progresses.inputs.hora_ini')
                            </th>
                            <th class="text-left">
                                @lang('crud.progresses.inputs.hora_fim')
                            </th>
                            <th class="text-left">
                                Dia
                            </th>
                            <th class="text-center">
                                @lang('crud.common.actions')
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        @forelse($progresses as $progress)
                        <tr>
                            <td>
                                {{ optional($progress->student)->nome ?? '-' }}
                            </td>
                            <td>
                                {{ optional($progress->car)->placa.' / '.optional($progress->car)->marca ?? '-' }}
                            </td>
                            <td>
                                {{ optional($progress->instructor)->nome ?? '-'
                                }}
                            </td>
                            <td>{{ $progress->hora_ini ?? '-' }}</td>
                            <td>{{ $progress->hora_fim ?? '-' }}</td>
                            <td>{{ \Carbon\Carbon::parse($progress->created_at)->format('d/m/Y') ?? '-' }}</td>
                            <td class="text-center" style="width: 134px;">
                                <div
                                    role="group"
                                    aria-label="Row Actions"
                                    class="btn-group"
                                >
                                    @can('update', $progress)
                                    <a
                                        href="{{ route('progresses.edit', $progress) }}"
                                    >
                                        <button
                                            type="button"
                                            class="btn btn-light"
                                        >
                                            <i class="icon ion-md-create"></i>
                                        </button>
                                    </a>
                                    @endcan @can('view', $progress)
                                    <a
                                        href="{{ route('progresses.show', $progress) }}"
                                    >
                                        <button
                                            type="button"
                                            class="btn btn-light"
                                        >
                                            <i class="icon ion-md-eye"></i>
                                        </button>
                                    </a>
                                    @endcan @can('delete', $progress)
                                    <form
                                        action="{{ route('progresses.destroy', $progress) }}"
                                        method="POST"
                                        onsubmit="return confirm('{{ __('crud.common.are_you_sure') }}')"
                                    >
                                        @csrf @method('DELETE')
                                        <button
                                            type="submit"
                                            class="btn btn-light text-danger"
                                        >
                                            <i class="icon ion-md-trash"></i>
                                        </button>
                                    </form>
                                    @endcan
                                </div>
                            </td>
                        </tr>
                        @empty
                        <tr>
                            <td colspan="8">
                                @lang('crud.common.no_items_found')
                            </td>
                        </tr>
                        @endforelse
                    </tbody>
                    <tfoot>
                        <tr>
                            <td colspan="8">{!! $progresses->render() !!}</td>
                        </tr>
                    </tfoot>
                </table>
            </div>
        </div>
    </div>
</div>
@endsection
